const express = requre("express");
const app= express();

// req, resquest and response

    app.get("/", function(req, res){

        throw new ErrorEvent("asadorennd");
        
    })
    app.listen(3000);